#include "main.h"

/**
 * _abs - to get the abs of any input
 * @n: value type of parameter
 *
 * Return: 0 as success
 */
int _abs(int n)
{
	if (n == 0)
	{
		return (0);
	}
	else if (n < 0)
	{
		return (n * -1);
	}
	else
	{
		return (n)

	return (0);
}
