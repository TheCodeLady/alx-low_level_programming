#include "main.h"

/**
 * _abs - to get the abs of any input
 * @int - value type of parameter
 *
 * Return: 0 as success
 */
int _abs(int)
{
	int n;
	if (n == +n || n == -n || n)
	{
		_putchar(n + '0');
	}

	return (0);
}
